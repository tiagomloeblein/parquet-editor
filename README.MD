# 🪶 Parquet Editor (Python + ttkbootstrap)

Editor visual simples, rápido e leve para arquivos `.parquet`, feito em **Python**, com interface moderna usando **ttkbootstrap**.

---

## ✨ Funcionalidades

- 🧩 **Edição por duplo clique** em qualquer célula  
- ➕ **Adicionar** e ➖ **Excluir linhas**
- 🔍 **Busca global instantânea**
- 🧭 **Paginação automática** (100 linhas por página)
- 🧱 **Filtro por coluna** (modo contém texto)
- 🧮 **Contador dinâmico de linhas e colunas**
- 💾 **Salvar** em `.parquet` novamente
- 🎨 Interface moderna baseada em temas do `ttkbootstrap`

---

## 🧰 Instalação

```bash
git clone https://github.com/tiagomloeblein/parquet-editor.git
cd parquet-editor
pip install -r requirements.txt
python editor_parquet.py
